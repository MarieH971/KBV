{% extends 'base.html.twig' %}

{% block title %}Modifier l'Adhérent
{% endblock %}

{% block body %}
	<h1>Modifier l'adhérent
		{{ user.lastName }}
		{{ user.firstName }}</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit" class="btn btn-warning">Mettre à jour</button>
	{{ form_end(form) }}

	<a href="{{ path('user_show', {'id': user.id}) }}" class="btn btn-secondary mt-3">Retour à l'adhérent</a>
{% endblock %}